<div class="mb-4">
    <label class="block text-gray-700 text-sm font-bold mb-2" for="{{ $name }}">
        {{ $name }}
    </label>
    <input class="shadow" id="{{ $name }}" type="checkbox" value="{{ $checkbox }}">
</div>
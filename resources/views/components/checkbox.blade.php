<div class="mb-4">
    <label class="block text-gray-700 text-sm font-bold mb-2" for="{{ $name }}">
        {{ $label }}
    </label>
    <input name="{{ $name }}" class="shadow" id="{{ $name }}" type="checkbox" value="{{ $checkbox }}">
</div>